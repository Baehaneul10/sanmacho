# API 베이스 (선택)
# - 비우면: 프로덕션 빌드에서 브라우저가 같은 도메인의 /api 로 요청 (nginx location /api/ 프록시 필요)
# - 개발: npm run dev 시 vite가 /api 를 127.0.0.1:3000 으로 프록시하므로 비워도 됨
# - API가 다른 호스트면: VITE_API_BASE=https://api.example.com
VITE_API_BASE=
